[project]
name = "aws-vuln-triage-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=1.0.0",
    "python-dotenv>=1.2.3",
    "pyyaml>=6.0.3",
]
