2026-08-17/Inspectorがある中、旗艦（自作）を作る必要性はあるか/Inspectorだけでは実現したいことができないため、旗艦に着工する/Inspectorは「該当バージョンが入っている」ところまではできても、実際の対応要否判断まではできない。2026-08-15の`ALAS2023-2025-1318`で、mod_cgid条件 → 自環境はmod_cgi側のため対応不要と判断できたのが価値。現在Inspector未導入のため、公開仕様と実例1件からの判断。

2026-08-17/今回作成するリポジトリはpublic・privateどちらを採択するか/publicリポジトリとする/後から公開するは整備コストなどの追加精査が必要になるため、公開前提で構築する（ai-news-collectorで経験済み）。非公開情報として扱うべきものである実データ（stack-profile.yaml）、.envはGit資材対象外とする。組織固有の事情等は抽象度をあげて書く方針で、データ等を理由にprivateに落とす判断は採択しない。

2026-08-17 / 短期ゴールの「誰かが使う状態」の定義 / 対象は自環境および自組織のサーバー（php, nginx, httpd）。入口はLLMツールが新着ALASトリガーでトリアージ判定結果を出力した時点。稼働開始 = ツールが新着 ALAS をトリガーに判定結果を初めて出力した日。出口は人間が検証を行い要対応または対応不要の判定結果と根拠をレポート化し管理者へ報告した時点。判定は稼働開始後の新着ALASを全件処理（3件未満なら過去分で補い最低3件）。判定期日は2026-10-31とする。制約としてAWS API接続は行わずstack-profile.yamlの手動記述のみを入力とする。 / 判定を着手ではなくツールの成果物出力とし手作業での達成を防止。判定を固定件数でなく全件処理＋最低補填数としALASの流量という外部要因依存を排除。実環境接続への脱線を防ぐため入力制約を明記。

2026-08-26/プロンプトに書いてよいのは「入力テキストの読解」だけ。計算と組織パラメータはコード側に置く

2026-08-31/ALAS2023-2025-1318.txtの出所/[alas.aws.amazon.com/AL2023/ALAS2023-2025-1318.html](https://alas.aws.amazon.com/AL2023/ALAS2023-2025-1318.html)

2026-09-06/実環境用と検証環境用のprofileをどう分けるか/実環境用・ローカル用・サンプルの3ファイルに分割管理する/検証のため実環境用のファイルを使い回し設定汚染を生じさせてしまった。実環境の設定汚染を防ぎつつ、検証用設定を安全にローカル実行できるようにするため

2026-09-06/判定に効く情報をYAMLコメントに書いて良いか/書かない。`usage_context`や明示的なキーとして持たせる/構造から外れたコメントが判断のきっかけになるのを防ぎ、ツールの解析対象からも外したいため

2026-09-06/プロンプトで【最優先】などの強調表現を使って良いか/使わない。強調したくなった場合はスキーマ設計の不足を疑う/責務の分離により強調が不要となり、構造的検証で品質を担保すべきだから

2026-09-06/コミットメッセージの規約をどうするか/Conventional Commitsに準拠(`feat:`,`fix:`, `docs:`, `chore:`, `refactor:`, `test:`)。迷ったら`chore:`とする/ログの視認性とツール連携を高めつつ、記述時の分類で作業が停滞するのを防ぐため

2026-09-09/LLMが提案した未知のキー名を、evalsの正解として採点して良いか/採点しない。statusを第1関門、missing_config_keysはstatus==要確認の時だけ網羅率で採点、それ以外は`[]`を要求。正解リスト外のキーは減点せず件数のみ記録/未知のキー名はn=3で3回とも別名だった(`archive/runs_20260909.log`)。名前で採点できない。ただし「採点できない提案が出た」という事実自体がスキーマ拡張のトリガーになる

2026-09-09/プロンプトやスキーマで語彙を縛らずに、出力のキー名を安定させられるか/できない。出力の名前が揺れたら、入力側の語彙不足を疑う/2026-09-06の「プロンプトで強調表現を使いたくなったら、まずスキーマ不足を疑う」と同じ構造。今回は強調ではなく語彙

2026-09-09/「対応手段の違いでurgencyを出し分けて良いか」/ **してはいけない。urgencyはリスク側の変数だけで決める。** 対応手段・再起動要否・調整リードタイムはランク→期日変換の側に置く/2026-08-15の分離の再合流になる。組織の運用体制が変わるたびに evals のラベルが全部無効になる`

2026-09-12/AWS APIで取得可能な静的情報（OS/バージョン等）を `stack-profile` に手動で記述するか/API未接続の現状においては「手動入力しない」原則の適用外とする。ただし将来のAPI連携を見据え、手動設定（`components`）とは明確に分離し、`inventory:` 名前空間へ隔離して定義する。/2026-09-12時点では実環境（API）と未接続であり、原則が防ぎたかった「二重管理の乖離リスク」が物理的に存在しないため。接続を開始した際は`inventory`の情報はprofileから削ぎ落とす。

2026-09-12/ツールの定義とコア価値の再確認/本ツールは「複雑な自然言語アドバイザリ本文（発動条件）」と「自組織の構成情報（事実）」の照合・判定に特化する。/脆弱性の検出や単純な深刻度スキャンはInspector等の既存ツールの領域であり、LLMの最大の強みである「非構造化テキストの解読と事実照合」に集中して差別化を図るため。

2026-09-12/LLM（判定層）とコード（合成・提示層）の完全分離/LLMには客観的事実（OS・パッケージ・構成情報）のみを入力し、組織パラメータ（criticality, usage_context等）をプロンプトから完全に除外する。最終緊急度の算出はコード側で行う。/主観情報の混入によるLLMの推論汚染とハルシネーションを根本排除し、組織の運用体制が変化しても評価データ（evals）の正解ラベルが揺れない再現性を確保するため。

2026-09-12/技術的深刻度（technical_severity）の決定方式/LLMによる自然言語での曖昧な深刻度推論（旧ルール5）を廃止し、外部取得したCVSS Base Scoreの数値比較によりコード側のルールで決定論的に算出する。/セキュリティツールとして判定の揺れを徹底的に排除し、「境界のある制約はコードに焼く」原則を適用するため。

2026-09-12/CVSS連携の導入タイミング（稼働開始枠の保護）/CVSSの外部API連携（NVD等）は稼働開始（2026-10-31）後の機能アップデート扱いとし、2026-09-14時点の稼働開始条件から切り離す。/ツールの核心（発動条件の判定）はCVSS無しで成立しており、2026-09-14の2時間枠で外部API連携まで盛り込むと設計が溢れ、2026-10-31の稼働開始日が遅延するため。

2026-09-12/自己評価（evals）におけるreason（判定根拠）の扱い/reasonは人間向け画面表示用にとどめ、evalsの自動採点対象から除外する。evalsはstatusとmissing_config_keysの完全一致のみで採点する。/自然言語表現の表記揺れによる自動テストの不確実性を排除し、評価の決定論的信頼性を確保するため。
